version: '1'
services:
  db:
    image: postgres
    restart: always
    shm_size: 128mb
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: "Sh@shank9868b"

  pgadmin4:
    image: elestio/pgadmin
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: shashank.singh2016.ss@gmail.com
      PGADMIN_DEFAULT_PASSWORD: "Sh@shank9868b"
      PGADMIN_LISTEN_PORT: 8080
    ports:
      - "8080:8080"
  portfolio-1: 
    build: .
    environment: 
      - APP_NAME=portfolio-1
    ports:
      - "3001:3000"

  portfolio-2: 
    build: .
    environment: 
      - APP_NAME=portfolio-2
    ports:
      - "3002:3000"

  portfolio-3: 
    build: .
    environment: 
      - APP_NAME=portfolio-3
    ports:
      - "3003:3000"